<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    
</head>
<body>
    
    <%- include('./parts/nav.ejs') %>


    <section>
        <% for( let i = 0; i < products.length; i++ ) { %>

      <div>
        <img width="200"  src="<%= products[i].image %>" alt="">
        <h2><a href="/products/<%= products[i]._id %>"> <%=products[i].title %></a></h2>
      </div>
        
        <% } %>

    </section>

</body>
</html>